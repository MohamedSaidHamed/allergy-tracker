// Re-export from the shared context so all screens use the same location state.
export { useLocation, LocationProvider } from "@/contexts/LocationContext";
export type { LocationPermissions } from "@/contexts/LocationContext";
